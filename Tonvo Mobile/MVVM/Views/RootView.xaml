<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    x:Class="Tonvo_Mobile.MVVM.Views.RootView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:model="clr-namespace:Tonvo.MVVM.Models"
    xmlns:viewModel="clr-namespace:Tonvo_Mobile.MVVM.ViewModels"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    Background="{StaticResource Background}"
    Title="Lists">

    <ContentPage.Resources>
        <Style TargetType="Grid">
            <Setter Property="VisualStateManager.VisualStateGroups">
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal" />
                        <VisualState x:Name="Selected">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor" 
                                        Value="{StaticResource Primary}" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList> 
            </Setter>
        </Style>
    </ContentPage.Resources>
    <Grid>
        <RefreshView RefreshColor="{StaticResource Primary}" 
                     IsRefreshing="{Binding IsRefreshing}"
                     Command="{Binding RefreshCommand}">
            <CollectionView ItemsSource="{Binding Vacancies}"
                            SelectionMode="Single"
                            SelectedItem="{Binding SelectedVacancy}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="10">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>

                            <Label Text="{Binding VacancyName}"
                                   TextColor="{StaticResource Font}"/>
                            <Label Grid.Row="1"
                                   Text="{Binding VacancySalary}"
                                   TextColor="{StaticResource Font}"/>
                            <Label Grid.Row="2"
                                   Text="{Binding CompanyName}"
                                   TextColor="{StaticResource Font}"
                                   VerticalOptions="End"/>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>
    </Grid>
</ContentPage>